// #include "common_external_includes.h"
#include "constants.h"

class Ship
{
    private:
        sf::CircleShape sprite;
        sf::RenderWindow& window;

    public:

        Ship(sf::RenderWindow& w);
};
